version: "3.9"
services:
  chatterfix-backend:
    build: .
    container_name: chatterfix-backend
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - GOOGLE_APPLICATION_CREDENTIALS=/app/creds/firestore_service_account.json
    restart: unless-stopped